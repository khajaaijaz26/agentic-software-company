{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.software-agent.dev/vnext/legacy-controller-lock.schema.json",
  "title": "Legacy Agent Company controller lock reader",
  "description": "Read-only compatibility shape accepted during controller migration. Software Agent never emits this schema.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema", "pid", "createdAt", "instanceId", "userBinding", "workspaceHash"],
  "properties": {
    "schema": { "const": "agent-company.controller-lock/v1" },
    "pid": { "type": "integer", "minimum": 1, "maximum": 9007199254740991 },
    "createdAt": { "type": "string", "format": "date-time" },
    "instanceId": { "type": "string", "pattern": "^ctl_[a-f0-9]{32}$" },
    "userBinding": { "type": "string", "pattern": "^[a-f0-9]{48}$" },
    "workspaceHash": { "type": "string", "pattern": "^[a-f0-9]{24}$" }
  }
}
