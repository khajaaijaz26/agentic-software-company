# Codex (~/.codex/config.toml)
# Add the block below to your existing config.toml under the [mcp_servers] table.

[mcp_servers.software-agent]
command = "software-agent-mcp"
args = []

# Install first with: python -m pip install "software-agent-compat[mcp]"
# From a source checkout, use: python -m pip install -e ".[mcp]"
